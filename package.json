{
  "name": "esy-solve-cudf",
  "version": "0.1.6",
  "description": "Solver command for esy based on mccs",
  "license": "GPLv3",
  "esy": {
    "build": "dune build -p #{self.name}"
  },
  "dependencies": {
    "@opam/cmdliner": "^1.0.2",
    "@opam/dune": "*",
    "@opam/cudf": "*",
    "@opam/mccs": "*"
  },
  "resolutions": {
    "@esy-ocaml/esy-installer": "0.0.1"
  },
  "peerDependencies": {
    "ocaml": "~4.6.0"
  },
  "devDependencies": {
    "@esy-ocaml/merlin": "*",
    "ocaml": "~4.6.0"
  }
}
